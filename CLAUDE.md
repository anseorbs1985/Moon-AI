# Moon-AI 프로젝트 — 클로드 공통 지침

## 지침 관리 규칙 (클로드 스스로 지킬 것)

- 사용자가 클로드의 행동 방식에 대한 **새 지침**을 내리면, 이 파일(CLAUDE.md)에 추가하고
  **자동으로 GitHub에 push** 한다 (따로 물어보지 않아도 됨). — 단, 아래 push 권한 규칙 적용.
- `git pull`을 받으면 이 파일의 최신 지침을 읽고 **그대로 따른다** — 어느 컴퓨터에서든 동일하게.

## push 권한 (메인 컴퓨터 구분) — 절대 규칙

- **메인 컴퓨터**: launcher 폴더의 `local_config.json`에 `"is_main": true` 가 있는 컴퓨터.
- **메인이 아닌 컴퓨터에서는 사용자가 명시적으로 "올려줘/push"라고 명령하기 전에는
  절대 GitHub에 push하지 않는다** (지침 자동 push 규칙도 메인에서만 적용).
  수정이 생기면 로컬 커밋까지만 해두고, push가 필요하면 사용자에게 알린다.
- **push하는 내용은 코드/지침뿐** — 좌표 파일은 2026-08-04부터 머신별 데이터라
  어떤 컴퓨터에서도 push하지 않는다 (아래 '좌표는 머신별' 규칙 참조).

## 업데이트는 수동으로만

- **자동 업데이트는 하지 않는다** (2026-07-21 사용자 지시로 비활성). 업데이트는 사용자가
  🔄 버튼을 눌렀을 때만 실행. 클로드가 자동 업데이트/자동 pull을 다시 켜지 말 것.

## "git pull" 한 마디 = 전체 업데이트 절차 (모든 컴퓨터 공통)

- 사용자가(또는 업데이트 창이 자동으로) **"git pull"** 이라고만 입력하면, 단순 pull이 아니라
  **업데이트 전체 절차**를 즉시 수행한다 (되묻지 않음):
  1. 좌표 파일 백업 확인 후 `git pull` (충돌 시 stash 백업 → 재시도 → 필요하면
     백업 브랜치 만들고 `git reset --hard origin/main`으로 원격 동기화)
  2. **좌표 파일(coords.json·island_coords.json)은 절대 건드리지 않는다** —
     이 컴퓨터의 바탕화면 좌표가 이 컴퓨터의 원본 (2026-08-04 머신별 정책)
  3. 코드 파일을 바탕화면(런처 폴더)으로 복사(배포)
  4. 워치독으로 메인런처 재시작 + **창을 띄워서 보여줌**
  5. push는 하지 않는다 (push 권한 규칙 그대로)
- 업데이트(🔄) 버튼은 자동복구(저장소 clone, stash 재시도, 원격 강제 동기화, 복사 재시도)를
  먼저 시도하고, 그래도 실패할 때만 클로드 창에 "git pull"을 입력해 넘긴다.

## 좌표 배포 — 업데이트(🔄)로 좌표 전부를 받는다 (2026-08-09 재변경)

- **coords.json·island_coords.json 둘 다** 메인 것을 받는다 (사용자 지시로 전체 배포 복귀).
- 프리셋 정의(`_doll_presets` 등)도 함께 넘어간다.
- 로컬이 자기 좌표를 지키고 싶으면 받기 전에 **[💾 좌표저장]**, 되돌릴 땐 **[♻ 좌표복구]**.
- 메인이 좌표를 push하고, **메인이 아닌 컴퓨터는 🔄 업데이트로 섬/던전 좌표를 받는다.**
- **메인 컴퓨터**는 자기 바탕화면 좌표가 원본 — 업데이트가 절대 덮어쓰지 않는다.
- 덮어쓰기 전에 현재 좌표를 `backups\<시각>_before_update_*` 로 자동 백업하고,
  로그에 "이전 좌표는 백업됨 — 되돌리려면 [♻ 좌표복구]" 를 남긴다.
- **로컬에서 자기 좌표를 지키는 방법**: 좌표를 맞춘 뒤 런처의 **[💾 좌표저장]** 을 눌러둔다.
  업데이트로 좌표가 바뀌어도 **[♻ 좌표복구]** 한 번이면 자기가 저장한 좌표로 돌아간다.
- 클로드는 사용자 요청 없이 좌표 파일을 수정/삭제하지 않는다(배포·복구 목적 제외).
- (2026-08-04의 '좌표 머신별·공유 중단' 정책은 로컬이 좌표를 못 받는 불편으로 폐지.)

## 사용자 답변 축약어 (모든 컴퓨터 공통)

**응 = ㅇㅇ = dmd = dd** → 전부 "응(yes)" 이라는 뜻이다.
(dmd = '응'을 영문 자판으로, dd = 'ㅇㅇ'을 영문 자판으로 친 것)

클로드가 "진행할까요?", "올릴까요?" 등으로 물었을 때 사용자가 위 답 중 하나를 치면
**재확인 없이 바로 그 작업을 진행**한다. 다른 맥락에서도 동일하게 긍정으로 해석한다.

## 저장소 규칙

- `accounts.json`(게임 계정 정보)과 `local_config.json`(머신별 설정)은 **절대 GitHub에 올리지 않는다** (gitignore 됨).
- `daya_counts.json`·`daya_history.json`(다야 **측정값**)은 **머신별 데이터** — `%LOCALAPPDATA%\MoonAI`에 저장, GitHub에 올리지 않고 업데이트가 절대 덮어쓰지 않는다.
- `daya_regions.json`(다야 OCR **캡처영역/좌표**)은 2026-07-31부터 **공유 대상** — 메인이 원본으로 push하고, 메인이 아닌 컴퓨터는 업데이트 때 `%LOCALAPPDATA%\MoonAI\daya_regions.json`으로 통째로 받는다 (로컬 유실 시에도 업데이트로 자동 복구됨).
- 좌표(coords.json, island_coords.json)는 **2026-08-04부터 머신별 데이터** — GitHub에 없음,
  push 금지, 업데이트가 절대 덮어쓰지 않음. 오림의일기장 이미지(reroll_templates/)는 공유 대상.
- 코드 수정 후에는 항상 바탕화면으로 복사(배포)하고 워치독(`LineageM_Watchdog` 예약 작업)으로 메인런처를 재시작한다.

## 실행 중에는 메인런처를 최소화한다 (2026-08-07 변경)

- **실행 버튼·단축키로 작업이 시작되면 메인런처를 최소화**한다(서브창·클로드도 함께).
  화면에 남아 있으면 사용자가 실수로 클릭할 수 있기 때문.
- **작업이 끝나면 앞으로 올리지 않고 '맨 뒤'로만 복원**한다(`_restore_back`) —
  리니지M 클라이언트를 가리지 않게. (2026-07-31의 '절대 최소화 안 함' 규칙은 이날 폐지)

## 메인런처는 절대 꺼진 채로 두지 않는다

- **새벽 4:50 자동 시작**: 예약 작업 `LineageM_AutoStart_0450`이 매일 04:50에 워치독을 실행해
  런처가 꺼져 있으면 되살린다(켜져 있으면 아무 일도 안 함). 업데이트(🔄) 때 없으면 자동 등록.
- **스케줄 10분 전 자동 시작**: 런처가 켜져 있는 동안 켜진 스케줄(과거섬 5:03, 우편 23:30 등)을
  확인해 **시작 10분 전 시각**으로 `LineageM_Pre_<이름>` 예약 작업을 자동 등록/해제한다.
  그 시각에 런처가 꺼져 있으면 워치독이 켜준다(이미 켜져 있으면 아무 일도 안 함).
  스케줄을 켜고 끌 때마다 자동 반영되므로 사용자가 따로 할 일은 없다.
- **10분마다 상시감시(KeepAlive)는 사용하지 않는다** (2026-08-09 사용자 지시로 폐지).
  업데이트 시 이 예약 작업이 있으면 삭제한다. 평소에 꺼져 있으면 사용자가 직접 켠다.
  **자동 실행은 새벽 4:50(`LineageM_AutoStart_0450`) 하나만 유지.**

- `git pull`·업데이트·좌표 편집 등으로 메인런처를 종료했으면, 작업이 끝난 뒤 **항상 다시 실행**한다.
  중간에 오류가 나더라도 **런처부터 먼저 다시 띄우고** 나서 오류를 보고한다.
- 종료가 필요한 작업(coords.json 편집 등)은 **종료 → 작업 → 재시작을 한 번에** 끝내고,
  사용자에게 "꺼져 있는 상태"를 남기지 않는다.
- 메인런처 [🔄 업데이트] 버튼도 동일 — 파일 복사가 실패해도 예외를 잡아 재시작 단계로 넘어가고,
  워치독 → 직접 실행 순으로 폴백하며 실제 기동까지 확인한다.
- **재시작한 런처는 최소화하지 말고 창을 띄워서 보여준다** — 사용자가 요청한 재시작/업데이트 후에는
  런처 창을 화면에 복원(SW_RESTORE)해 보여줄 것. 이후 정리는 10분 유휴 자동 최소화가 담당하고,
  불필요하면 사용자가 직접 최소화한다. (단, 새벽 워치독 자동 시작(04:00/04:50)은 최소화 유지)

## 💾 좌표저장 / ♻ 좌표복구 버튼 — 로컬에서 가장 중요한 기능 (2026-08-05)

- 메인런처 [🔄 업데이트] 옆에 **[💾 좌표저장] / [♻ 좌표복구]** 가 있다. 컴퓨터마다 자기 좌표를
  `%LOCALAPPDATA%\MoonAI\usersave` 에 저장해두고, 문제가 생기면 그 시점으로 되돌린다.
- **사용법**: 좌표를 맞춰놓을 때마다 [💾 좌표저장]을 눌러둔다 → 좌표가 꼬이면 [♻ 좌표복구] 한 번.
  복구하면 런처가 자동 재시작된다(메모리의 옛 좌표가 다시 저장되는 것을 막기 위해 필수).
- **이 기능은 반드시 동작해야 한다.** 클로드는 이 코드를 고칠 때 아래 보호장치를 절대 없애지 말 것:
  - 빈/깨진 좌표는 저장 자체를 거부 (복구본 오염 방지)
  - 저장은 임시파일 → 바이트 검증 → 교체, 최근 20세대 `usersave\history` 보관
  - 복구 시 저장본이 손상됐으면 history의 최근 정상본으로 자동 대체
  - 복구 직전 현재 좌표도 `backups\<시각>_before_restore_*` 로 백업
  - 섬/던전·OCR 종료 후 복사, 복사 3회 재시도 + 바이트 검증, 재시작은 워치독→직접실행 폴백
- 사용자가 "좌표 복구해줘"라고 하면: `usersave` → `usersave\history` → `backups` 순으로
  가장 최근 정상본(좌표 개수로 판단)을 찾아 복원하고 런처를 재시작한다.

## 좌표 자동 백업·복구 (모든 컴퓨터 공통)

- 좌표 파일(coords.json·island_coords.json·local_config.json)은 **런처 시작 시 하루 1회** +
  **업데이트 실행 시마다** `%LOCALAPPDATA%\MoonAI\backups` 에 자동 백업된다 (약 한 달치 보관).
- 사용자가 "**좌표가 날아갔다/초기화됐다**"고 하면: 이 백업 폴더에서 **가장 최근의 정상 백업**
  (파일 크기·등록 좌표 개수로 판단)을 찾아 런처 실행 폴더의 coords.json 등으로 복원하고
  런처를 재시작해줄 것. git stash 목록(`git stash list`)에 '업데이트 자동백업'이 있으면 그것도 확인.

## 클로드 창은 자동으로 앞에 띄우지 않는다 (2026-08-09)

- 작업이 끝나도 클로드 창을 **앞으로 올리지 않는다**. 사용자가 메인런처의
  **[💬 클로드]** 버튼(계정관리 왼쪽)이나 작업표시줄로 직접 꺼낸다.
- 실행 중에 클로드를 내리는 것(`_minimize_claude`)은 그대로 유지 — 클릭을 가리면 안 되므로.

## 업데이트는 저장소의 코드 전부를 받는다 (2026-08-09)

- 업데이트(🔄)는 목록에 적힌 파일뿐 아니라 **저장소의 모든 .py/.pyw**를 자동으로 받는다
  (파일을 새로 만들고 목록에 적는 걸 잊어서 배포가 누락되는 사고 방지).
- 코드 복사는 **바이트 단위로 검증**하고 실패하면 파일별로 3회까지 재시도한다.
  그래도 실패하면 업데이트를 실패로 처리해 재시도/클로드 마무리 경로로 넘긴다.

## 클로드 창 최소화는 그대로 둔다 (2026-08-09)

- 런처의 `_minimize_claude`는 두 번 정의돼 있고, 앞쪽 정의에 있는
  **"승인 대기 3분간 최소화 안 함"** 보호는 뒤 정의에 덮여 동작하지 않는다.
  **이건 사용자가 원하는 상태 — 버그로 보고 고치거나 통합하지 말 것.**
- 이유: 클로드 창은 항상 위(topmost)라, 그 보호가 살아 있으면 스케줄·2시간 반복이
  시작될 때 클로드가 화면을 덮은 채 좌표 클릭이 들어가 씹히거나 엉뚱한 곳에 눌린다.
- **실행·스케줄·반복이 시작되면 런처·서브창·클로드를 전부 내리고(최소화/맨뒤로) 시작한다.**
  (메인런처는 `_start_pause()`, 섬/던전 실행기는 `_test`/`_start`의 `_send_behind_main()`)

## 업데이트는 좌표를 자동 저장하지 않는다 (2026-08-09 최종)

- 업데이트 직전 자동 저장 기능은 **삭제됨** (사용자 지시 — 수동으로만).
- 좌표를 지키려면 사용자가 런처의 **[💾 좌표저장]** 을 직접 눌러둔다.
- 클로드는 이 자동 저장을 다시 넣지 말 것.
## 좌표는 배포하지 않고 '시간'만 공유한다 (2026-08-09 재변경 — 최종)

- 컴퓨터마다 클라이언트 위치가 달라 **좌표(coords.json·island_coords.json)는 업데이트로
  배포하지 않는다.** 로컬 좌표가 그 컴퓨터의 원본.
- 대신 **클릭 간격(gap_list)만** 메인 것으로 맞춘다 — `share_times.json` 의 `keys` 에 적힌
  던전만 대상. 좌표·이름·녹화·방향은 전혀 건드리지 않는다.
- 시간을 공유하고 싶으면: 메인에서 간격을 맞춘 뒤 `island_coords.json` 과
  `share_times.json`(대상 던전 목록)을 push → 로컬은 🔄 업데이트만 누르면 된다.
- 특정 던전만 공유하려면 `share_times.json` 의 `keys` 를 그 던전만 남긴다.

## 섬/던전 실행은 최소화하지 않고 '맨 뒤'로만 (2026-08-09 사용자 지시)

- 섬/던전 실행기(악몽의섬 등)가 시작될 때 **메인런처도 섬 실행기도 최소화하지 않는다.**
  둘 다 **맨 뒤(HWND_BOTTOM)** 로만 보낸다 — 최소화하면 다시 꺼내기가 번거롭다.
  (런처: `_island_step_back()`, 섬 실행기: `_send_behind_main()`)
- 클로드 창은 그대로 최소화한다(항상 위라 클릭을 가림).
- 런처 자체 작업(인형탐험·우편함 등)의 최소화 동작은 기존대로 둔다.

## ⏰ 반복은 '종료'만 있다 — 확인창 없음 (2026-08-09)

- [⏰ 반복] 버튼을 누르면 **묻지 않고 즉시** 2시간 N회 반복이 전부 꺼진다
  (`repeat_h=0`, 예약 파일 비움). **일시정지·재개 개념은 없다.**
- **[■ 전체멈춤]과 [⏰ 반복]은 따로다 (2026-08-15 사용자 지시로 분리)**:
  - **[■ 전체멈춤]** = 지금 돌고 있는 작업·별도 런처 프로세스만 종료 (⏰ 예약은 그대로 둔다).
  - **[⏰ 반복]** = ⏰ 2시간 N회 예약만 끈다.
  - 예전엔 전체멈춤이 반복까지 껐는데, 버튼을 둘로 만든 뜻이 없어져서 분리했다.
- 다시 하려면 섬/던전 실행기(또는 메인런처 악몽의섬 판)에서 ⏰를 다시 켠다.

## 좌표는 '지정한 항목만' 내보낸다 (2026-08-09)

- 기본은 좌표 배포 안 함. 다만 `share_coords.json` 의 `keys` 에 적은 **coords.json 항목만**
  메인 좌표를 받는다 (예: `["fish_slots"]` → 낚시녹임 좌표만).
- 나머지 좌표(메인런처 클릭·과거섬·우편 등)와 `island_coords.json` 은 **로컬 것 그대로**.
- 내보낼 때: 메인에서 좌표를 찍은 뒤 `coords.json` + `share_coords.json` 을 push →
  로컬은 🔄 업데이트만 누르면 그 항목만 받는다.
- 더 이상 공유하고 싶지 않으면 `share_coords.json` 의 `keys` 를 비운다.

## 프리셋은 녹화를 지우지 않는다 (2026-08-10 — 사고 재발 방지)

- 프리셋의 **'✖삭제'** 는 그 자리의 **좌표·방향만** 지운다. **녹화(⏺)는 절대 지우지 않는다.**
  대신 `recs_off` 에 숨겨 보관하고, 그 자리를 다시 쓰게 되면(그대로/위치변경) 자동 복원한다.
- 2026-08-09 잊혀진섬에서 프리셋 적용 한 번으로 **12슬롯의 클릭23 녹화가 통째로 사라진** 사고가
  있었다 (`rc.pop(str(j), None)` 때문). 클로드는 이 보호를 절대 없애지 말 것.
- 녹화가 사라졌다는 신고가 오면: `git log -- island_coords.json` 으로 녹화 개수가 줄어든
  커밋을 찾아 그 직전 버전의 `recs` 만 현재 파일에 되살린다(좌표·간격은 건드리지 않음).

## 실행은 사용자가 시킬 때만 — 자동은 없다 (2026-08-10 사용자 지시, 최우선 규칙)

- **사용자가 [실행]을 누르지 않으면 아무 작업도 시작되지 않는다.** 사용자가 끄면 전부 꺼진다.
- 클로드는 **어떤 작업도 스스로 시작하지 않는다** — 자동 반복 등록, 자동 재개,
  꺼져 있던 스케줄·⏰반복 되살리기, 진단용 임의 실행 전부 금지.
  진단·테스트로 실행이 필요하면 **반드시 먼저 물어본다.**
- **예외 — 런처를 '켜두는' 예약은 그대로 유지한다** (사용자가 원함):
  `LineageM_AutoStart_0450`(4:50 런처 기동), `LineageM_Pre_<이름>`(스케줄 10분 전 기동),
  워치독 재시작. 이건 **런처 창을 살려두는 것일 뿐 클릭 작업을 시작하지 않는다.**
- 사용자가 직접 켜둔 스케줄(과거섬 5:03·우편 23:30 등)과 ⏰ 반복은 정상 동작이다.
  다만 클로드가 그것을 새로 켜거나 되살리지 않는다.
- **[■ 전체멈춤] = 완전 종료** — 실행 중 작업, 섬/던전 실행기·다야 OCR 등 별도 런처
  프로세스, ⏰ 반복 예약까지 전부 끈다. **멈췄다 다시 이어가는 개념은 없다.**

## 던전 단위 공유 — share_island.json (2026-08-10)

- `island_coords.json` 중 **`share_island.json` 의 `keys` 에 적은 던전만** 메인 것을 통째로 받는다
  (좌표·간격·이름·녹화 전부, `presets: true` 면 그 던전 프리셋도).
- 나머지 던전과 다른 파일은 **로컬 것 그대로**. 지금 대상: `월요일_잊혀진섬`.
- 공유 파일 3종 정리:
  - `share_times.json` — 그 던전의 **간격(gap_list)만**
  - `share_island.json` — 그 던전을 **통째로** (좌표·녹화 포함)
  - `share_coords.json` — `coords.json` 의 **지정 항목만** (예: `fish_slots`)
- 공유를 멈추려면 해당 파일의 `keys` 를 비운다.

## push는 '이번에 고친 것'만 (2026-08-11 사용자 지시)

- 작업이 끝나면 **그 작업으로 바뀐 파일만** 골라서 커밋·push 한다.
  `git add -A` 로 전부 쓸어 담지 말 것.
- **좌표 파일(coords.json·island_coords.json)은 배포할 때 항상 함께 올린다**
  (2026-08-15 변경 — 예전엔 "좌표도 올려줘"라고 할 때만 올렸다).
  새 런처를 만들어 좌표를 찍었으면 `share_coords.json` 의 `keys` 에도 그 항목을 추가해
  로컬이 🔄 업데이트로 실제로 받도록 한다.
- 무엇을 올렸는지 사용자에게 파일 이름으로 알려준다.

## 악몽의섬 ⏰ 2시간 6회는 고정 (2026-08-14 사용자 지시)

- `토요일_악몽의섬` 은 **횟수를 다 채워도 ⏰가 꺼지지 않는다** — 6회를 끝내면 자동으로
  다시 6회를 채워 넣는다 (`REPEAT_FIXED` 목록, `_rep_turn_off` 는 이 던전을 건드리지 않음).
- 이유: 6회가 끝날 때마다 반복이 풀려서 사용자가 매번 다시 켜야 했다.
- **[■ 전체멈춤]·[⏰ 반복] 로 끄는 것은 그대로 동작한다** (사용자가 끄면 꺼진다).
  다시 켜려면 섬/던전 실행기에서 ⏰를 켠다.
- **런처를 켠다고 반복이 켜지지는 않는다 (2026-08-16 사용자 지시 — 자동 켜기 삭제).**
  `_rep_arm_fixed`(시작 시 자동 등록)는 **제거했다. 다시 넣지 말 것.**
  ⏰는 **사용자가 악몽의섬을 실행했을 때**(슬롯 실행·선택실행) 또는 **⏰ 버튼을 켰을 때만** 걸린다.
  그래서 다른 컴퓨터(로컬)도 코드만 업데이트하면 바로 같은 설정으로 돈다
  (`island_coords.json` 을 주고받을 필요 없음).
- **개별로 실행하면 그때부터 다시 센다** — 섬/던전 실행기에서 슬롯을 직접 누르면
  그 슬롯의 반복이 `지금 + 2시간`, 남은 횟수 6회로 초기화된다 (`_rep_restart`).
  반복 관리가 자동으로 돌린 실행(`--slots`/`--slot`)은 해당 없음.
- **값은 창 위 '전체 일괄'이 이긴다 (2026-08-19 최종).** `_rep_hn()` 은 슬롯에 저장된
  `repeat_h`/`repeat_n` 을 먼저 쓰고, 비어 있을 때만 `REPEAT_FIXED` 기본값(2시간 6회)을 쓴다.
  `REPEAT_FIXED` 는 '횟수를 다 채워도 안 꺼지고 다시 채운다'는 성질에만 관여한다.
- 다른 던전을 고정하고 싶으면 `REPEAT_FIXED = {던전: (몇시간, 몇회)}` 에 추가한다
  (런처·섬 실행기 양쪽에 같은 값이 있어야 한다).

## 여기서 한 작업은 로컬에서도 그대로 (2026-08-15 사용자 지시, 중요)

- 메인에서 정한 **동작·설정은 다른 컴퓨터(로컬)에서도 똑같이 되어야 한다.**
  로컬이 🔄 업데이트만 누르면 같은 상태가 되도록 만든다.
- 그러려면 설정을 **머신별 파일에만 저장하지 말 것**:
  - `island_coords.json`·`coords.json` 은 **그 컴퓨터의 데이터** — 여기에만 넣으면 로컬엔 안 간다.
  - 그래서 '어디서나 같아야 하는 설정'은 **코드에 넣는다** (예: `REPEAT_FIXED` — 악몽의섬
    2시간 6회는 런처가 켜질 때 코드가 자동으로 켜준다).
  - 코드로 넣기 어려우면 `share_times.json` / `share_island.json` / `share_coords.json` 에
    항목을 추가해 공유 대상으로 만든다.
- **예외는 좌표뿐** — 클라이언트 위치가 컴퓨터마다 달라 좌표는 각자 것을 쓴다
  (공유가 필요하면 사용자가 "좌표도 올려줘"라고 할 때만).
- 작업을 마치면 "로컬에서도 이대로 되는가?"를 한 번 확인하고, 안 되면 코드로 옮긴다.

## 🔒 좌표 잠금 — 잠근 것은 업데이트가 절대 못 건드린다 (2026-08-17 사용자 지시)

- 메인런처 [♻ 좌표복구] 아래 **[🔒 좌표잠금]** 버튼 → 런처별/던전별로 잠글 수 있다.
- 잠금은 **그 컴퓨터에만** 저장된다: `%LOCALAPPDATA%\MoonAI\coord_lock.json`
  (`keys` = coords.json 항목, `island` = island_coords.json 던전). **GitHub와 무관**.
- `lineagem_update.py` 의 `sync_coord_keys` / `sync_island_keys` / `sync_times` 는
  **잠긴 항목을 건너뛴다** — `share_*.json` 에 적혀 있어도 덮어쓰지 않는다.
  업데이트 로그에 `🔒 잠금 — 건드리지 않음: …` 이 남는다.
- **클로드는 이 잠금을 우회하거나 무시하는 코드를 만들지 말 것.** 사용자가 직접
  풀기 전에는 그 좌표가 이 컴퓨터의 원본이다.

## ⏺ 녹화만 따로 배포 — share_recs.json (2026-08-17)

- `share_recs.json` 의 `keys` 에 적은 **던전·클릭 번호의 녹화(⏺)만** 메인 것을 받는다.
  좌표·이름·간격·방향은 **전혀 건드리지 않는다.** 예: `{"화요일_에카": [23]}`
- `ignore_lock: true` 면 **🔒 좌표잠금과 무관하게 녹화만 전달된다** (사용자 지시 —
  잠금은 좌표를 지키는 것이고, 녹화는 좌표가 아니므로 통과시킨다).
  좌표는 잠금 그대로 보호되고, 업데이트 로그에 그 사실이 남는다.
- 공유 파일 4종 정리:
  - `share_times.json` — 그 던전의 **간격(gap_list)만**
  - `share_island.json` — 그 던전을 **통째로**
  - `share_coords.json` — `coords.json` 의 **지정 항목만**
  - `share_recs.json` — 그 던전의 **지정 클릭 녹화만**

## ↩ 되돌리기 지시서 — restore_order.json (2026-08-19)

- 로컬이 **🔄 업데이트할 때 '한 번만'** 좌표를 되돌린다 (같은 `id` 는 그 컴퓨터에서 재실행 안 됨,
  `LOCALAPPDATA/MoonAI/restore_done.json` 에 기록).
- `mode: "last_run"` — **던전마다 '그 던전을 마지막으로 실행했을 때'의 좌표로 각각** 되돌린다.
  던전별로 시점이 달라도 각자 맞춰 고른다. 순서: `runsnap`(실행 직전 스냅샷) → `backups`.
- `keep_recs: true` — **녹화(⏺)는 지금 것을 그대로 둔다.** 좌표·이름·간격·방향만 되돌린다.
- 실행 직전 스냅샷: 섬/던전 실행기가 실행 때마다 그 던전 좌표를
  `LOCALAPPDATA/MoonAI/runsnap/<던전>_<시각>.json` 에 남긴다(던전당 최근 10개).
- **업데이트는 좌표 파일을 통째로 덮어쓰지 않는다** (2026-08-17 사고 후 차단).
  좌표가 필요하면 `share_*.json` 으로만, 그것도 🔒 잠금이 우선이다.

## 마우스 분리 — 이 런처들은 커서를 쓰지 않는다 (2026-08-19 사용자 지시)

- **용던고고·스케줄·인형탐험·변신확인용·인형확인용·성물확인용·아이템정리·낚시녹임**
  (그리고 서커스 3종·거래소검색)은 **커서를 움직이지 않는 클릭**(창에 메시지 전달)만 쓴다.
  사용자가 그 사이에 마우스로 다른 일을 해도 서로 방해하지 않는다.
- '마우스만 올려놓는 자리'(스케줄 클릭2 앞)는 `move_at()` — 커서 대신 WM_MOUSEMOVE 만 보낸다.
- 이 런처들은 **마우스 움직임 감지로 멈추지 않는다** (`NO_WAIT_MOUSE`). 예전엔 마우스를
  움직이면 "⏸ 마우스 움직임 감지"로 기다렸는데, 커서를 안 쓰므로 기다릴 이유가 없다.
- **예외 — 진짜 커서가 필요한 것**: 아이템정리의 '위로 쓸어올리기'(드래그),
  절전해제(F11), 녹화 재생(키보드), 붙여넣기 자리 클릭. 이때는 잠깐 커서를 쓴다.
- 클로드는 이 런처들에 `pyautogui.click/moveTo` 를 다시 넣지 말 것 — `click_at()`/`move_at()` 사용.

## ↩ 되돌리기 버튼 — 로컬에서 클릭 한 번으로 (2026-08-19)

- 메인런처 [🔒 좌표잠금] 아래 **[↩ 되돌리기]** 버튼 → 창이 뜨고 던전별로 보여준다:
  `토요일_악몽의섬  실행 08-16 02:24 → 08-15 17:13 [백업]  16슬롯 → 16슬롯`
  (이미 같으면 회색으로 '이미 같음', 자료가 없으면 빨간 글씨로 이유)
- 체크한 던전만 **그 던전을 마지막으로 실행했던 때의 좌표**로 되돌린다.
  **녹화(⏺)·다른 던전·coords.json 은 그대로.** 직전 상태는 자동 백업 후 런처 재시작.
- 원격으로 로컬을 여러 대 다룰 때 콘솔 없이 화면에서 확인·복구하려고 만들었다.
- 같은 일을 하는 콘솔 도구: `restore_lastrun.py` (`py restore_lastrun.py` → 확인, `go` → 실행)

## 섬/던전 창의 '전체 일괄' 줄 (2026-08-19 사용자 지시)

- 잊섬·에카·오만·악몽 창 위쪽에 **[전체 일괄]** 칸이 있다. 여기서 한 번만 정하면
  **좌표가 등록된 모든 슬롯**이 같아진다 (컴퓨터·슬롯마다 하나씩 고칠 필요 없음).
  - **⏰ [n]시간 [m]회 → [⏰ 반복 켜기(전체)]** : 모든 슬롯에 반복을 걸고 예약도 즉시 등록
    (`_bulk_repeat`). **[⏰ 반복 끄기(전체)]** 는 그 던전 반복만 전부 해제.
    오른쪽에 **'지금 2시간 6회' / '지금 반복 없음'** 이 표시돼 실수로 켜는 것을 막는다.
    켜고 끈 것은 `repeat_log.txt` 에 '전체 일괄 — … (사용자)' 로 남는다.
  - **🧪 [프리셋 ▾] → [전체 적용]** : 고른 프리셋(주홍이/빨갱이 등)을 모든 슬롯에 적용
    (`_bulk_preset`) — 슬롯 이름·좌표가 그 프리셋으로 바뀌고 물약 매칭 표시도 바로 갱신.
- 이 버튼들은 **사용자가 누를 때만** 동작한다 (자동 실행 아님).

## 클릭은 '마우스 분리(SendInput)' 하나만 쓴다 — 가상 클릭 삭제 (2026-08-21 사용자 지시)

- 클릭은 `pyautogui.click` 을 쓰고, 그 함수는 `precise_click.install()` 이 **SendInput** 으로
  바꿔둔 것이다 — **[이동+누름+뗌]이 한 번에 전송**돼 실행 중 사용자가 마우스를 움직여도
  그 사이에 끼어들지 못하고 클릭은 항상 지정 좌표에 찍힌다. 이게 사용자가 원하는 '마우스 분리'다.
- **창에 메시지만 보내는 '가상 클릭'(PostMessage — post_click/post_move/post_wheel)은 삭제했다.**
  커서를 아예 안 쓰는 대신, 상단바(ngptop)·옆 패널(CEFCLIENT) 같은 창을 못 찾거나 게임이
  가짜 입력을 무시해서 **클릭이 씹혔다**(스케줄·용던고고 등에서 확인).
  **클로드는 이 방식을 다시 넣지 말 것.** `NO_CURSOR_CLICK` 은 False 고정.
- 그래서 실행 중에는 예전처럼 **사람이 마우스를 놓을 때까지 기다린다**(`_wait_mouse_idle`).

## 사람이 마우스를 '클릭'하는 것도 감지한다 (2026-08-21 — 팅김 방지)

- 예전에는 **커서 위치가 변했는지**만 봐서, 사용자가 움직이지 않고 **클릭만** 하면 감지하지
  못하고 그 위에 자동 클릭이 겹쳐 들어갔다(= 한 번씩 팅김).
- 이제 `precise_click.start_input_watch()` 가 **저수준 마우스 훅(WH_MOUSE_LL)** 으로
  **물리 입력만** 본다 — 우리가 보낸 SendInput 클릭은 `LLMHF_INJECTED` 로 걸러져 세지 않는다.
  `idle_seconds()`(사람이 만진 뒤 지난 시간) · `button_down()`(지금 누르고 있는가) 제공.
- 런처는 **좌표를 누르기 직전마다** `_wait_user_free()` 로 확인한다.
  대상: **용던고고·흑기사·매일매일 스케줄**(`CLICK_GUARD`) — 사용자가 제일 중요하게 보는 것들.
  손을 떼면 **즉시** 이어서 클릭하고, 30초 넘게 붙잡고 있으면 그냥 진행한다.
- **키보드도 같이 본다 (2026-08-21 추가)** — 사용자가 키를 누르는 중에 자동 클릭이 들어가도
  팅긴다. `WH_KEYBOARD_LL` 훅으로 물리 키만 감지하고, 우리가 보낸 키(F11 절전해제·녹화
  재생 등)는 `LLKHF_INJECTED` 로 걸러진다. 키를 누르고 있는 동안에도 클릭을 미룬다.
  뗌 신호를 놓쳐 '계속 눌림'으로 굳는 것을 막으려고 5초 넘게 조용하면 눌림 상태를 비운다.
- 훅을 못 켜는 환경이면 조용히 예전(위치 비교) 방식으로 되돌아간다.

## 자동 클릭 뒤엔 사용자가 보던 창으로 즉시 되돌린다 (2026-08-21)

- 커서 클릭은 그 창을 **앞으로 끌어올린다(포커스 뺏김)** — 사용자가 다른 클라에서
  캐릭터를 움직이는 중이면 **이동이 끊기고 그 창으로 넘어가 버린다.**
- 그래서 클릭 직전 `_user_focus_snap()` 으로 **사용자가 보던 게임 창 + 커서 위치**를 기억하고,
  클릭 직후 `_user_focus_back()` 으로 **원래 창을 다시 앞으로, 커서도 제자리로** 돌린다.
  (사용자가 8초 안에 마우스·키보드를 만졌고, 그 창이 게임 창일 때만 — 아니면 아무 것도 안 한다)
- **적용 시점**: 용던고고·흑기사는 **좌표 하나 누를 때마다**,
  매일매일 스케줄은 **슬롯의 클릭1 직후**와 **'좌표2 이동 → 좌표3 클릭' 묶음이 끝난 뒤**
  (묶음 중간에 되돌리면 올려둔 자리가 풀려서 클릭2가 씹힌다).
- 클로드는 이 복귀를 없애지 말 것 — 사용자가 게임하면서 돌리는 것이 기본 사용법이다.

## 악몽의섬 — 첫 회차만 4시간, 그 다음부터 2시간 (2026-08-22 사용자 지시)

- 반복을 **새로 걸 때만** 첫 대기를 4시간으로 잡는다. 6회 = **4시간 1회 + 2시간 5회**.
  `REPEAT_FIRST = {"토요일_악몽의섬": 4}` (런처·섬 실행기 양쪽에 같은 값).
- 적용되는 곳: 슬롯 실행으로 반복이 걸릴 때, ⏰ 토글로 켤 때, 창 위 '전체 일괄'로 켤 때.
  **한 회차를 돌고 난 뒤 다음 예약은 평소 주기(2시간)** — 첫 회차에만 쓰인다.
- **슬롯마다 클릭해서 돌려가며 고른다 (2026-08-22 사용자 지시)** — 슬롯 칸의 모드 버튼을
  누를 때마다 **[4h→2h] → [2h마다] → [4h마다]** 로 순환한다 (`NIGHT_MODES`).
  - **그 슬롯만** 바뀐다. **이미 돈 횟수(남은 회)는 그대로 이어가고** 다음 실행 시각만 다시 잡는다
    — 한 번이라도 돈 슬롯은 평소 주기로, 아직 안 돈 슬롯은 첫 회차 규칙으로.
  - 모드는 `island_repeat.json` 의 `_mode`(`"던전|슬롯": first|h2|h4`)에 저장되고,
    호환을 위해 `_first` 도 같이 갱신한다. 그 슬롯의 `repeat_h` 도 2/4로 맞춰 저장한다.
  - 섬/던전 실행기는 `_first` 를 보고 같은 규칙으로 돈다.
- **16슬롯을 한 번에 다시 거는 버튼 (2026-08-22)** — 악몽의섬 판 머리줄:
  - **[⏰ 4h→2h 6회]** : 평소 시작 — 첫 회차 4시간 뒤, 이후 2시간 간격 6회
  - **[⏰ 2h 6회]** : **돌던 중에 팅겼을 때** — 지금부터 2시간 간격으로 6회
    (4시간을 또 기다리면 시간이 어긋나므로)
  둘 다 `repeat_h` 를 항상 2시간으로 맞추고 예약만 다시 잡는다. **실행은 하지 않는다.**
  **주의 — 이 두 버튼은 16슬롯을 전부 초기화한다** (남은 횟수도 6회로). 돌던 중인 슬롯을
  건드리고 싶지 않으면 슬롯 칸의 모드 버튼으로 하나씩 바꾼다.
- **이 기능은 악몽의섬에만 적용한다** (사용자 지시) — 다른 던전은 건드리지 않는다.
  `REPEAT_FIRST` 에 항목이 없는 던전은 예전 그대로 평소 주기로만 돈다.

## 악몽의섬 슬롯마다 ✕ 로 반복 취소 (2026-08-22 사용자 지시)

- 메인런처 악몽의섬 판의 각 슬롯 `⏰` 옆에 **빨간 ✕** 버튼 — 그 슬롯의 반복만 취소한다.
  `⏰` 는 예전처럼 끄기/다시 켜기 토글이고, **✕ 는 끄기 전용**(다시 켜지 않는다).
- 취소하면 예약 파일에서 그 슬롯을 지우고 `island_coords.json` 의 `repeat_h` 도 0으로 만든다.
  다른 슬롯·실행 중인 작업은 건드리지 않는다. `repeat_log.txt` 에 '반복 취소 ✕ (사용자)' 로 남는다.

## 악몽의섬 기본값과 🔄 초기화 (2026-08-22 사용자 지시 — 고정)

- **악몽의섬의 기본값은 "4시간 → 2시간 6회"** (첫 회차 4시간, 두 번째부터 2시간씩, 총 6회).
  초기화하면 **항상 이 값**이 된다. 다른 값으로 돌리고 싶으면 사용자가 수동으로 바꾼다.
- **[🔄 초기화 (4h→2h 6회)] 버튼이 두 군데 있다** — 둘 다 같은 동작:
  - 메인런처 악몽의섬 판 제목 아랫줄 (`_night_rearm_all(True)`)
  - 악몽의섬 실행기 창의 '전체 일괄' 줄 (`_night_reset_default`)
  16슬롯의 **설정만** 되돌린다 (`repeat_h=2 / repeat_n=6`, 모드 `first`).
  **걸려 있던 예약은 지우고, 새로 걸지 않는다** — 반복은 사용자가 실행했을 때만 걸린다.
- **토요일 지나면 자동 초기화는 삭제했다 (2026-08-23 사용자 지시 — 다시 넣지 말 것).**
  초기화가 반복을 켜는 셈이 되어 '사용자가 실행할 때만 켠다' 규칙을 어겼다.
- 개별 슬롯만 다르게 하려면 슬롯 칸의 모드 버튼을 눌러 `4h→2h / 2h마다 / 4h마다` 로 바꾼다
  (그 슬롯의 남은 횟수는 유지된다).

## 어두운 화면 (눈 보호) — 2026-08-22 사용자 지시

- 메인런처 [↩ 되돌리기] 아래 **[☀ 밝게] / [🌙 어둡게]** 버튼. 설정은 `coords.json` 의
  `dark_ui` 에 저장되고 **기본값은 어두운 화면**. 런처가 켜질 때 자동 적용되고,
  새로 여는 서브창에도 그대로 적용된다 (`_open_section_win` → `apply_dark`).
- `apply_dark()` 는 **색을 일부러 지정해둔 버튼(빨강·초록 등)은 건드리지 않는다.**
  흰 배경·어두운 글씨만 골라 바꿔 대비를 지킨다 (밝기 계산으로 판단).
- 클로드는 새 화면을 만들 때 배경색을 흰색으로 고정하지 말 것 — 그러면 어두운 화면에서 튄다.

## 악몽의섬 — 개별 실행은 2시간 6회가 기본 (2026-08-22 사용자 지시)

- **슬롯을 직접 실행하면(메인런처 슬롯 [실행], 섬 실행기 슬롯 실행) 지금부터 2시간 간격 6회**로
  건다. 4시간으로 시작하고 싶으면 사용자가 슬롯 모드 버튼에서 `4h→2h` 를 고른다.
- 슬롯 모드는 **`4h→2h` ↔ `2h마다` 두 가지뿐** — `4h마다` 는 의미가 없어 삭제했다(다시 넣지 말 것).
- 첫 회차 4시간은 **초기화(🔄)로 걸 때**와 **토요일이 지나 자동 초기화될 때** 적용된다.

## ⏰ 반복은 '사용자가 실행했을 때만' 켜진다 (2026-08-23 사용자 지시 — 절대 규칙)

- **반복(⏰)을 켜는 유일한 경로는 사용자가 그 던전을 실행한 것이다** — 전체실행·선택실행·개별실행,
  또는 사용자가 직접 누르는 ⏰ 버튼(슬롯 토글 / '⏰ 반복 켜기(전체)'). 악몽의섬·에카 전부 동일.
- **초기화(🔄)는 값만 정한다.** 4시간→2시간 6회로 설정을 되돌리고 **걸려 있던 예약은 지운다.**
  절대 새로 걸지 않는다. 실행하면 그때 그 설정대로 걸린다.
- **슬롯 모드 버튼([4h→2h]/[2h마다])도 켜지 않는다.** 이미 걸려 있으면 그 예약의 다음 시각만
  고치고, 꺼져 있으면 설정만 바꾼다.
- **`island_coords.json` 에 `repeat_h` 가 남아 있어도 자동으로 등록하지 않는다.**
  예전 `_island_repeat_check` 는 '설정만 있고 예약이 없으면' 새로 걸었는데, 이것이
  자동으로 반복이 켜지는 통로였다 — **제거했다. 다시 넣지 말 것.**
- 실행했을 때 첫 대기는 **그 슬롯의 모드대로** — `4h→2h` 면 4시간, `2h마다` 면 2시간.
- **실행으로 반복을 걸 때는 `island_coords.json` 의 `repeat_h`/`repeat_n` 도 함께 되살린다
  (2026-08-23 — 한 번만 돌고 끝나던 버그의 원인).** [반복 종료]·✕ 는 `repeat_h` 를 0으로
  만드는데, 그 상태로 실행하면 예약은 걸려도 **1분 뒤 반복 관리자가 '꺼진 슬롯'으로 보고
  그 예약을 지워버려서** 한 번만 돌고 끝났다. `_night_rep_restart`(런처)와
  `_rep_restart`(섬 실행기) 둘 다 주기를 되살린다. 이 저장을 빼지 말 것.

## 메인런처 슬롯판 — 던전 4개 탭 (2026-08-24 사용자 요청)

- 악몽의섬 슬롯판을 **탭으로 넓혔다** — `[악몽의섬] [에카] [잊혀진섬] [오만의탑]`.
  탭을 누르면 그 던전의 16슬롯이 그 자리에 나와 **개별 실행·선택실행**을 할 수 있다.
  판 크기는 그대로(자리를 더 차지하지 않는다). 마지막에 본 탭은 `coords.json` 의
  `night_tab` 에 저장된다.
- `NIGHT_KEY`/`NIGHT_DIDX` 가 탭에 따라 바뀌므로 실행·⏰·✕·모드 버튼이 그대로 따라간다.
- **다른 던전(에카·잊섬·오만)은 실행해도 반복을 새로 걸지 않는다** — 그 슬롯에
  사용자가 `repeat_h` 를 켜둔 경우에만 걸린다. 악몽의섬만 실행 시 2시간 6회가 걸린다.
- 🔄 초기화·[⏰ 2h 6회] 버튼은 **악몽의섬 탭에서만** 동작한다 (다른 탭이면 안내만 뜬다).
- 좌표·물약·층수·프리셋 등 설정은 **각 던전 창 그대로** — 슬롯판은 실행만 한다.

## 📏 요약 런처 — lineagem_bar.pyw (2026-08-24 사용자 요청)

- **메인런처 전체를 켜지 않고 '악몽의섬 판 딱 그 정도'만** 띄우는 작은 항상-위 창 (321×322).
  메인런처 **[🏝 섬/던전 실행기] 바로 아래 [📏 요약런처 (던전 4개)]** 버튼으로 띄운다.
  **별도 프로세스**라 요약런처의 **✕ 는 그 창만 닫는다** — 메인런처·돌던 작업·⏰ 반복은 그대로.
  이미 떠 있으면 새로 띄우지 않는다 (`bar.json` 의 `pid` 로 확인).
- **탭**: 악몽의섬 / 에카 / 잊혀진섬 / 오만의탑 — 마지막 탭은 `bar.json` 에 저장.
- 슬롯마다 **[nn 실행]** · **[2h n회][✕]** · **[4h→2h]/[2h마다]** · **[+]**, 위에 **[✔ 전체선택] [▶ 선택실행] [해제] [⌖]**
  (⌖ = 그 던전 좌표·설정 창 열기, 실행 안 함). 악몽의섬 탭에서만 **[🔄 초기화] [⏰ 2h 6회]**.
- **반복 규칙은 메인런처와 동일** — 실행하면 악몽의섬만 2시간 6회가 걸리고
  (`repeat_h`/`repeat_n` 도 되살림), 다른 던전은 반복을 새로 켜지 않는다.
  ✕ 는 그 슬롯 반복만 취소, 🔄 초기화는 설정만 되돌리고 예약은 지운다.
  반복 상태는 메인런처와 **같은 파일**(`island_repeat.json`)이라 양쪽에 똑같이 보인다.
- **◐** 는 '섬/던전 실행기 창'의 투명도 (100/85/70/55/40%, `ui.json` 의 `island_alpha`).
  **≡** 드래그로 이동, **✕** 로 닫기. 20초마다 슬롯 상태를 새로 읽는다.
- **메인런처 악몽의섬 판과 같은 동작**을 하도록 맞췄다 (2026-08-24):
  실행 중이면 **대기열에 쌓았다가 하나씩** 돌린다(겹쳐 돌면 클릭이 엉킨다 — 메인런처와 동일),
  슬롯마다 첫 대기 방식 **[4h→2h]/[2h마다]** 버튼, 반복·모드는 같은 파일을 본다.
  다른 점: 실행할 때 메인런처·클로드 창을 내리는 동작은 없다(별도 프로세스라 화면을 안 건드림).
- 좌표·물약·층수·프리셋 등 설정은 **각 던전 창 그대로** — 요약 런처는 실행/반복만 다룬다.

## 실행만 할 때는 던전 창을 아예 띄우지 않는다 (2026-08-24 사용자 지시)

- `lineagem_island.py` 를 `--run`/`--slot`/`--slots` 로 켜면 **창을 `withdraw()` 해 안 띄운다.**
  예전엔 띄웠다가 맨 뒤로 보냈는데, **그 순간 부하가 걸려 렉이 생겼다**.
  `_send_behind_main()` 은 숨겨진 창이면 아무것도 하지 않는다.
  (2026-08-09 '맨 뒤로만 보낸다' 규칙은 **좌표 창을 직접 열었을 때**만 해당)
- 요약런처(`lineagem_bar.pyw`)는 **실행을 누르면 스스로 맨 뒤로 내려갔다가**
  (`-topmost` 해제 + `lower()`) **작업이 끝나면 다시 앞(항상 위)으로** 돌아온다.
  항상 위로 떠 있으면 그 자리의 좌표 클릭을 이 창이 먹어버리기 때문. 최소화는 하지 않는다.
- 요약런처 대기열에 들어간 슬롯은 실행 버튼이 **주황색 `대기1`** 로 바뀐다(메인런처와 동일).
- 클릭 씹힘 방지: 순차 실행에서도 **다른 클라로 넘어갈 때 그 창을 먼저 앞으로 올리고**
  0.25~0.45초 뒤에 누른다 (비활성 창의 첫 클릭은 '창 띄우기'로만 먹히고 사라진다).

## 요약런처는 '항상 위'가 아니다 — 누르면 앞으로 (2026-08-24 사용자 지시)

- 요약런처는 **항상 위로 두지 않는다.** 다른 창을 누르면 뒤로 물러나고,
  **메인런처의 [📏 요약런처] 버튼을 누르면 앞으로 올라온다**
  (버튼은 새로 띄우는 대신 `bar.json` 의 `raise` 시각만 갱신 → 요약런처가 1초마다 보고 올라옴).
  창 자체를 클릭해도 올라온다.
- **실행 중인지 판단은 표시 파일 하나로 즉시** — 섬/던전 실행기가 `--run` 으로 켜지면
  `LOCALAPPDATA/MoonAI/island_run.json` 에 `pid` 를 남기고 끝나면 지운다.
  요약런처는 그 pid 가 살아 있는지 커널에 바로 물어본다.
  **예전엔 `wmic` 로 프로세스를 뒤져서 버튼이 1~3초씩 멈췄다 — 다시 쓰지 말 것.**

## 휠 지정 — 칸수 + [▲]/[▼] 방향 (2026-08-24)

- `opts=True` 런처(서커스 등)의 좌표 칸에는 **간격(초)** 과 **휠**을 직접 넣을 수 있다.
  휠은 **칸수 입력칸 + [▲]/[▼] 방향 버튼** — 아래로 굴리려면 ▼ 로 바꾼다.
  저장은 부호로 남는다(아래=음수). 상태 표시는 `휠▼100` / `휠▲5`.
  (예전엔 0~9 드롭다운뿐이라 큰 값도 아래 방향도 못 넣었다)
- **큰 값(±10 이상)은 3칸씩 나눠서** 여러 번 보낸다 — 한 번에 크게 보내면 게임이 무시한다.
- (2026-08-26) '🖤 흑기사 지정' 런처는 한 번 쓰고 **완전히 삭제했다** — 코드·설정키
  `knight2_slots`·버튼·좌표잠금 항목까지 전부. 휠 개선만 남겼다.

## 🖼 이미지로 찾아 클릭 — 용던고고 (2026-08-26 사용자 요청)

- 좌표 칸의 **[🖼]** 버튼 → 화면에서 **드래그로 그 그림을 잘라 저장**한다
  (`click_templates/dragon_05.png` 처럼 런처·좌표번호로 저장. 오른쪽 클릭 = 그림 삭제).
  그림이 있으면 버튼이 **[🖼있음]** 보라색으로 바뀐다.
- 실행할 때 그 자리에서는 **좌표 대신 그림을 찾아 그 위치를 누른다.**
  찾는 범위는 **그 좌표가 있는 리니지M 창 안**(창을 못 찾으면 좌표 주변 500×500).
  일치도 기준 `IMG_MATCH = 0.80`, `cv2.matchTemplate` 사용.
- **못 찾으면 그 슬롯만 거기서 끝낸다** (사용자 지시) — 다음 좌표를 누르지 않고
  **다른 슬롯은 그대로 계속** 진행한다. 웨이브는 `st["j"] = nclk` 로 그 슬롯만 종료,
  순차는 그 슬롯 루프만 `break`.
- 찾음/못 찾음은 상태줄과 `click_log.txt` 에 일치도까지 남는다.
- **찾을 범위를 지정한다 — [📐]** (2026-08-26). 드래그한 자리가 **어느 클라 창 안인지 보고
  그 창 왼쪽위 기준(dx,dy,w,h)으로 저장**하므로, 슬롯(클라)이 달라도 각 창의 같은 자리를 훑는다.
  범위를 안 잡으면 그 클라 창 전체를 훑는다. 오른쪽 클릭 = 범위 삭제.
  `click_templates/dragon_05_area.json`
- **일치 기준은 좌표마다 따로 — [🔍0.80]** (2026-08-26). 작은 그림은 일치도가 낮게 나와서
  기준을 낮춰야 잡힌다. 버튼을 **누르면 지금 찾아보고 큰 팝업으로 결과**(찾음/못찾음,
  최고 일치도, 찾은 자리, 훑은 범위)를 보여주고, 못 찾았으면 **'기준을 n.nn 로 낮추기'**
  버튼이 함께 뜬다. **오른쪽 클릭 = 기준 0.90→0.85→0.80→0.75→0.70→0.60 순환.**
  `click_templates/dragon_05_thr.json`
- **좌표를 등록하지 않아도 그림만 지정하면 그 자리를 실행한다** (2026-08-27 수정).
  예전에는 좌표가 없는 칸을 통째로 건너뛰어서 "그림을 넣었는데 클릭을 안 한다"는 문제가 있었다.
  그림만 있는 자리는 **같은 슬롯의 첫 등록 좌표(`slot_anchor`)로 어느 클라인지 판단**하고,
  그 창 안(또는 지정 범위)에서 그림을 찾아 **찾은 자리**를 누른다.
  그래서 그림 자리는 좌표와 위치가 달라도 된다. 단, **그 슬롯에 좌표가 하나도 없으면
  어느 클라인지 알 수 없어 실행하지 못한다.**
- **그림은 두 가지로 쓰인다 — 그 자리에 좌표가 등록돼 있는지로 갈린다 (2026-08-27 사용자 지시)**
  - **좌표 없음 + 그림 있음** → 그림을 찾아 **그림 가운데를 클릭**한다.
    게임 화면 안 아이콘은 커서를 먼저 올려야 잡히므로 `move_at` 으로 0.35~0.6초 올린 뒤 누른다.
  - **좌표 있음 + 그림 있음** → 그림은 **'창이 떴는지 확인'용**. 그림이 보일 때까지 기다렸다가
    **등록한 좌표**를 누른다 (창이 뜨기 전에 눌러 어긋나는 것을 막는다).
- 어느 쪽이든 **못 찾으면(최대 `IMG_TRIES`=4번, 1~1.5초 간격) 그 슬롯은 거기서 끝** —
  다음 좌표를 누르지 않고 다른 슬롯만 계속한다.
- **창이 이미 떴으면 재시도하지 않는다 (2026-08-27 사용자 지시)** — 그림을 못 찾았을 때
  **다음 자리의 그림(= 눌러서 뜨는 창)이 이미 보이면** 그 자리는 이미 눌린 것이므로
  더 찾지 않고 바로 다음 좌표로 넘어간다(`"이미열림"`). 이미 성공했는데 4번을 더 기다리며
  헛도는 것을 막는다. 창 확인용 자리(좌표가 등록된 자리)에는 적용하지 않는다.
- **일치 기준은 0.55** (2026-08-27 사용자 지시 — 최종). 그 그림이 뜨는 자리가 한 군데뿐이라
  낮춰도 오탐이 없다. 실측: 아이콘/창이 **없을 때** 16클라가 `dragon_05` 0.27~0.30 ·
  `dragon_06` 0.08~0.29 → **0.55 를 넘는 것 0개, 여유 0.25**.
  **있을 때**는 배경에 따라 0.60~0.96 으로 흔들리므로 이보다 높이면 진짜를 놓친다.
  (0.80 → 0.60 → 0.72 를 거쳐 0.55 로 정착. 기준만 만지지 말고 아래 '배수 판정'과 함께 볼 것.)
- **'반응 없으면 다시 클릭'은 넣지 말 것** — 창이 떠도 아이콘이 남아 있는 경우가 있어
  두 번 눌리는 문제가 생겼다 (2026-08-27 제거).
- **그림은 작고 안 변하는 부분만 자른다 (2026-08-27 — 실측)**. 배경·애니메이션 테두리가
  들어가면 배경이 다른 클라에서 점수가 확 떨어진다.
  같은 아이콘을 40×31(테두리+배경 포함) 과 18×17(안쪽 아이콘만) 로 비교한 결과:
  **있을 때 0.829 → 0.983, 없을 때 0.27 → 0.28** — 작게 자른 쪽이 구분이 훨씬 뚜렷하다.
- **한 슬롯에서 이미지 검색은 한 번만** (2026-08-27 사용자 지시) — 한 번 찾아 클릭했으면
  그 슬롯의 뒤 좌표에서는 검색하지 않고 좌표만 누른다 (`self._img_done`, 실행마다 초기화).
  같은 그림을 또 찾아 눌러서 되돌아가는 것을 막는다.
- **🎯 버튼** — 같은 그림이 여러 개 보일 때 `최고일치 / 맨위 / 맨아래 / 맨왼쪽 / 맨오른쪽`
  중 어느 것을 누를지 고른다 (`_pick.json`). 위·아래에 같은 아이콘이 있을 때 쓴다.
- 색으로 못 찾으면 **윤곽선(Canny) 매칭**으로 한 번 더 찾는다 — 배경이 달라도 모양은 같으므로.
- 그림은 저장소 폴더(`click_templates/`)에 있어 업데이트로 함께 배포된다
  (해상도·클라 크기가 같은 컴퓨터끼리만 그대로 쓸 수 있다).

## 웨이브는 좌표1~3까지만 — 이미지 구간은 슬롯 완주 (2026-08-27 사용자 지시)

- 용던고고는 **좌표1~3만 2슬롯 교차(웨이브)** 하고, **좌표4부터는 그 슬롯을 끝까지 밀어붙인다**
  (`WAVE_UNTIL = {"dragon": 3}` · `_wave_tail()`). 그 사이 다른 슬롯은 끼어들지 않는다.
- 이유: 좌표4~6은 **그림 찾기 → 아이콘 클릭 → 확인창 뜸 → 좌표 클릭** 이 이어지는 구간이라,
  중간에 다른 클라를 클릭하면 **포커스가 넘어가 확인창이 닫히거나 클릭이 씹힌다.**
  좌표3까지는 서로 독립적이라 교차해도 안전하다.
- 다른 런처에 같은 것이 필요하면 `WAVE_UNTIL` 에 `런처키: 좌표번호` 를 추가한다
  (숫자는 '교차를 허용할 좌표 개수' — 3이면 좌표1~3까지 교차).

## 창이 안 뜨면 2회까지 더 길게 다시 누른다 (2026-08-27 사용자 지시)

- 그림을 찾아 누른 뒤 **1.8~2.4초 기다렸다가 '다음 자리의 그림'(= 떠야 할 창)을 확인**하고,
  **없으면 다시 누른다 — 최대 2회, 갈수록 더 길게 꾹** (0.18~0.30초 → 0.35~0.50초).
  로그에 '눌렀는데 창이 안 뜸 → 다시 누름 (n/2)' 이 남는다. 로컬에서 이 증상이 잦았다.
- **예전에 금지한 재클릭과 다르다.** 금지한 것은 *'아이콘이 아직 보이니 다시 누른다'* 로,
  창이 떠 있어도 아이콘이 남아 두 번 눌렸다. 지금은 **창 자체가 없을 때만** 다시 누르므로
  두 번 눌릴 수 없다 (창이 있으면 재클릭 자체를 안 한다).
- 근거(2026-08-27 실측): 좌표5를 일치도 0.80~0.82로 정확히 눌렀는데 확인창이 **0.20** —
  즉 인식·시간 문제가 아니라 **게임이 클릭을 안 먹은 것**. 이때 스스로 회복하라는 장치다.

## 그림은 크기를 바꿔가며 찾는다 — 컴퓨터가 달라도 (2026-08-27)

- 메인에서 자른 그림을 로컬에서 쓰면 **해상도·클라 창 크기가 달라 그림이 조금 크거나 작게**
  보여서 점수가 떨어진다 (로컬 성공률 50% 신고의 유력한 원인).
- 그래서 기준을 못 넘으면 **0.80~1.25배로 9단계 확대·축소하며 다시 찾는다.**
  기준을 넘는 순간 멈춘다. 측정: 한 번 찾는 데 최악 **0.24초** (창 491×276 기준) — 부담 없음.
- 그래도 안 되면 **윤곽선(Canny) 매칭**이 마지막으로 한 번 더 시도한다.

## 🩺 이미지진단 버튼 — 로컬 문제는 이것부터 (2026-08-27)

- 그림이 지정된 런처(용던고고 등) 버튼줄에 **[🩺 이미지진단]** — 누르면 **지금 16클라를 전부
  훑어** 슬롯별·좌표별 일치도를 표로 보여준다 (초록 = 잡힘, 빨강 = 못 잡음).
- 맨 위에 **클라 창 크기**를 함께 보여주고, 크기가 서로 다르면 빨간 경고를 띄운다 —
  그림이 안 잡히는 가장 흔한 원인이라 먼저 확인시킨다.
- 결과는 `click_log.txt` 에 `[진단] …` 으로도 남아 원격에서 확인할 수 있다.
- **주의: 그 그림이 지금 실제로 화면에 떠 있어야 초록이 나온다** (던전 안이 아니면 전부 빨강).

## 실행 결과는 기록에만 남긴다 — 창은 띄우지 않는다 (2026-08-27 사용자 지시)

- **클로드는 사용자가 누르지 않은 창을 띄우지 말 것.** 매번 닫아야 해서 불편하다.
  실행이 끝나도 요약창을 띄우지 않는다 (2026-08-27 — 잠깐 넣었다가 이 지시로 제거).
- 대신 **상태줄에 한 줄** (`⚠ … 잘 안 된 슬롯 n개`) 과 **`click_log.txt` 의 `[결과]` 줄**로만
  알린다 (`self._run_note` → `_run_summary()`). 자세한 내용은 [📋 클릭기록] 에서 본다.
- 사용자가 **직접 누른** 버튼의 창(🔍 이미지 찾기 결과 · 🩺 이미지진단)은 그대로 둔다 —
  누른 사람이 결과를 기다리는 것이므로 다르다.
- 같은 내용이 `click_log.txt` 에 `[결과] …` 로도 남아 **원격에서 로컬 상태를 확인**할 수 있다.
- 기록되는 것: `좌표n 그림 못 찾음 (최고 0.xx)` · `좌표n 창이 안 뜸` ·
  `좌표n 창이 안 떠서 다시 누름` · `좌표n 두 번 눌러도 창이 안 뜸`
- 클로드는 이 기록을 없애지 말 것 — 로컬 문제를 원격으로 짚는 유일한 통로다.

## 그림은 컴퓨터마다 자기 것을 더 넣는다 (2026-08-27 — 로컬 '그림 못 찾음' 대책)

- **훑는 범위를 좁혀도 '못 찾음'은 안 고쳐진다.** 일치도는 '그 자리에서 얼마나 닮았나'라
  **범위 크기와 무관**하다 (실측: 창 전체 491×276 와 그 안 293×173 이 똑같이 0.2228).
  📐 범위는 **오탐 방지·속도**용이지 인식률용이 아니다. 클로드는 이걸 혼동하지 말 것.
- 진짜 원인은 **그림 자체가 그 컴퓨터 화면과 다른 것** (해상도·클라 크기·그래픽 설정).
- 그래서 한 좌표에 **그림을 여러 장** 둘 수 있다 — **하나라도 맞으면 찾은 것**으로 본다:
  - **공용 1장** — 저장소 `click_templates/`, 업데이트로 모든 컴퓨터에 배포
  - **이 컴퓨터 전용 최대 4장** — `%LOCALAPPDATA%\MoonAI\click_templates\<런처>_<번호>_mineN.png`
    **업데이트가 절대 덮어쓰지 않는다.** GitHub에도 올리지 않는다.
- **[🖼] 버튼 동작**: 공용 그림이 없으면 → 공용으로 저장. 이미 있으면 → **이 컴퓨터 전용으로 추가.**
  그래서 **로컬은 🖼 를 눌러 자기 화면에서 아이콘을 찍기만 하면 된다.**
  버튼에 장수가 보인다 (`🖼` / `🖼있음` / `🖼3장`).
- **오른쪽 클릭 = 삭제**: 전용 그림이 있으면 **전용만 전부** 지운다(공용은 남는다).
  전용이 없을 때만 공용을 지운다 — 실수로 공용을 날리지 않게 한 단계 둔 것이다.
- 찾는 순서: 공용 → 전용1 → … → 전용4. 각 장마다 **0.8~1.25배 크기 보정**과
  **윤곽선(Canny) 매칭**이 그대로 적용된다.

## 그림 판정은 '점수'가 아니라 '얼마나 튀는가'로 (2026-08-27 실측 — 중요)

- **절대 점수만으로는 자를 수 없다.** 같은 아이콘도 배경(지형·몹·이펙트)에 따라
  **0.60 ~ 0.96** 으로 흔들린다 (22:19~22:22 실행 13슬롯 실측). 기준을 0.72로 두니
  0.60·0.68 짜리 진짜 아이콘을 놓쳤고, 낮추면 오탐이 늘었다.
- 반면 **'1등 점수 ÷ 2등 점수'는 확실히 갈린다** (실측):
  - 아이콘 **있을 때** — 0.606 vs 0.272 = **2.23배**
  - 아이콘 **없을 때** — 16클라 전부 **1.00~1.20배** (1등과 2등이 붙어 있다)
- 그래서 판정 규칙: **점수 ≥ 기준** 이거나,
  **점수 ≥ `PEAK_MIN`(0.45) 이고 배수 ≥ `PEAK_RATIO`(1.6)** 이면 찾은 것으로 본다.
  2등은 1등 주변(템플릿 크기)을 지운 뒤 고른다 — 같은 아이콘을 2등으로 세지 않으려고.
- 검증: 놓쳤던 0.606 화면 → **찾음 ✔ (배수 2.23)**, 아이콘 없는 16클라 → **오탐 0개**.
- 클로드는 이 배수 판정을 빼지 말 것. 기준(🔍)만 만지면 이 문제는 다시 돌아온다.

## 그림 파일은 한글 경로에서도 읽어야 한다 (2026-08-27)

- `cv2.imread()` 는 **한글이 든 경로를 못 읽고 조용히 None 을 돌려준다.**
  윈도 사용자 이름이 한글인 컴퓨터에서는 **모든 그림이 '못 찾음'** 이 된다.
- 그래서 `cv2.imdecode(np.fromfile(path, np.uint8), ...)` 로 읽는다. 되돌리지 말 것.

## 마름모는 움직이지 않는다 — 날개만 애니메이션 (2026-08-27 사용자 정정)

- 클로드가 '마름모가 움직여서 클릭이 빗나간다'고 추측했으나 **틀렸다.**
  사용자 확인: **가운데 큰 마름모는 고정**이고 **옆의 날개 같은 것만 움직인다.**
- 이것이 같은 아이콘의 점수가 **0.60~0.96 으로 흔들리는 진짜 이유**다 —
  템플릿에 날개 영역이 조금이라도 들어가면 프레임마다 점수가 달라진다.
  → 대책은 **기준을 0.55 로 낮추는 것**과 **배수 판정**이지, 위치 보정이 아니다.
- 그림을 다시 자를 일이 있으면 **날개를 뺀 가운데 마름모만** 자를 것.
- 누르기 직전 위치 재확인(`DRIFT_MAX` 40px)은 남겨둔다 — 화면이 밀렸을 때를 위한
  보험이고 보통 0px 이 나온다. 값이 크게 찍히면 그건 화면이 밀린 것이다.

## 실패한 슬롯의 증거는 이름을 붙여 남긴다 (2026-08-27)

- 증거 사진이 슬롯마다 덮어써져서 정작 실패한 슬롯을 못 보던 문제 →
  파일 이름에 **슬롯 이름**을 붙인다:
  - `_누른자리_dragon_05_<슬롯>.png` — 무엇을 눌렀는지 (빨간 동그라미)
  - `_창안뜸_dragon_05_<슬롯>.png` — 두 번 눌러도 창이 안 떴을 때 그 슬롯 화면 전체
  - `_못찾음_dragon_05.png` — 못 찾았을 때 훑은 화면
- [📁 그림폴더] 버튼으로 열어 확인한다.
